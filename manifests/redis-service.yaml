apiVersion: v1
kind: Service
metadata:
  name: catalog-redis
  namespace: catalog-tronghuy0104
spec:
  selector:
    app: catalog-redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP