apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: catalog-tronghuy0104
spec:
  selector:
    app: catalog-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP